//Justin Fortner
//ID#1481947
//jfortner
//CE121 Class HW#1
//HW1.c

#include<string.h>
#include<stdio.h>
#include <stdlib.h>

typedef struct{
	char *name; // Your name as string
	char *grade; // Your letter grade as string, can be "A", "A-", "B+", "B", "B-", "C+", "C", "C-", "D", "F"
} hw_grade;

int main(int argc, char * argv[]) {
	//compare_grades(s1, s2);
}

int compare_grades(hw_grade *student1, hw_grade *student2) {
	/* returns 1 when student1’s grade is higher than student2’s grade,
	 0 when grades are equal,
	 and -1 when student1’s grade is lower than student2’s grade.
	*/
	return 0;
}

